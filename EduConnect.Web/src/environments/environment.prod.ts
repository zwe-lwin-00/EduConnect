/**
 * Production config. Set apiUrl and timezone per deployment (e.g. replace at build/deploy time).
 */
export const environment = {
  production: true,
  apiUrl: 'https://api.educonnect.com/api',
  timeZone: 'Asia/Yangon',
  timeZoneOffset: '+0630',
};
