export const environment = {
  production: false,
  // Update this to match your API port (check console when starting API)
  apiUrl: 'http://localhost:5049/api'  // HTTP profile from launchSettings.json
  // apiUrl: 'https://localhost:7289/api'  // HTTPS profile (if using HTTPS)
  // apiUrl: 'http://localhost:5000/api'   // Alternative HTTP port
};
