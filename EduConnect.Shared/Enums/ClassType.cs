namespace EduConnect.Shared.Enums;

/// <summary>
/// Type of class/session: one-to-one (teacher + single student) or group (teacher + multiple students).
/// </summary>
public enum ClassType
{
    OneToOne = 0,
    Group = 1
}
